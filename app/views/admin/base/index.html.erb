<div class="page-header">
	<h1><%= controller.controller_name.capitalize %></h1>
</div>

<form class="filters form-horizontal">
	<div class="well">
		<% @columns.each do |column| %>
			<!-- NOTE: all @columns must be in the DOM, even those that are not used to filter.
				that's why I'm using display:none -->
	  	<div class="control-group" style="<%= column[:filter] ? '' : 'display:none;' %>">
		    <label class="control-label" for="<%= column[:db_name] %>"><%= column[:human_name] %></label>
	    	<div class="controls">
		      <input type="text" id="<%= column[:db_name] %>" placeholder="<%= column[:human_name] %>">
	    	</div>
	  	</div>
	  <% end %>
 	</div>
</form>


<table id="<%= "#{controller.controller_name}_list" %>" class="datatable table table-striped table-bordered table-hover" data-source="<%= "/admin/#{controller.controller_name}.json" %>">
	<thead>
		<tr>
			<% @columns.each do |column| %>
				<th class="<%= column[:sortable] === false ? 'no_sort' : '' %>"><%= column[:human_name] %></th>
			<% end %>
		</tr>
	</thead>	
	<tbody></tbody>
</table>
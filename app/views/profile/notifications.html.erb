<div><%= render 'sub_nav' %></div>

<%= form_for @user, :url => update_password_profile_path, :method => :post, :html => { :id => 'basic_form' } do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<input type="checkbox" value="1"> I receive news updated, replies or private messages
  </div>
  <div class="actions">
    <%= f.submit "Update Notifications" %>
  </div>
<% end %>


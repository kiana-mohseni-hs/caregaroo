<%= form_for Post.new do |f| %>
  <div class="field">
    <%= f.label :content, "Share an update" %><br />
    <%= f.text_area :content, :size => "60x5" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
